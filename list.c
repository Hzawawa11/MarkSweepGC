#include <stdio.h>
#include <stdlib.h>
#include "structure.h"
#include "basic.h"

#define ListError(n) (n < 1)
Object* ListMake(int n){
  if(ListError(n)){
    return NULL;
  }
  return NULL;
}

void ListDisp(Object* list){

}

Object* ListDelete(int n){
  if(ListError(n)){
    return NULL;
  }
  return NULL;
}

Object* ListInsert(int n, Object* cell){
  if(ListError(n)){
    return NULL;
  }
  return NULL;
}